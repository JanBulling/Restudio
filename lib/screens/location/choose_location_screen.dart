import 'package:flutter/material.dart';

class ChooseLocationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Chosse Location Screen"),
      ),
    );
  }
}
